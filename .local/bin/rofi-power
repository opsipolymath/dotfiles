#! /bin/sh

chosen=$(printf "Power Off\nRestart\nLock" | rofi -location 3 -dmenu -i -p "Power Options")

case "$chosen" in
	"Power Off") poweroff ;;
	"Restart") reboot ;;
	"Lock") loginctl lock-session ;;
	*) exit 1 ;;
esac
